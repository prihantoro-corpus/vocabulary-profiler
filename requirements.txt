streamlit>=1.26.0    # Fixes the @st.cache_data error
pandas               # Data handling
openpyxl             # Reads and writes .xlsx files
fugashi              # Japanese tokenizer
lexicalrichness      # Lexical metrics (TTR, HDD, MTLD)
unidic-lite          # CRITICAL: Provides the dictionary, resolving the MeCab initialization error
